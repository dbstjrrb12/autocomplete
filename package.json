{
  "name": "autocomplete",
  "version": "1.0.0",
  "description": "autocomplete with Javascript",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "live-server": "^1.2.1"
  }
}
