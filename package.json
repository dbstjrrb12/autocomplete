{
  "name": "autocomplete",
  "version": "1.0.0",
  "description": "autocomplete with Javascript",
  "main": "index.js",
  "scripts": {
    "start": "tsc && live-server --port=8000"
  },
  "dependencies": {
    "live-server": "^1.2.1",
    "typescript": "^4.6.2"
  }
}
