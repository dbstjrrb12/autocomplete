{
  "name": "autocomplete",
  "version": "1.0.0",
  "description": "autocomplete with Javascript",
  "main": "index.js",
  "scripts": {
    "start": "live-server --port=8000"
  },
  "dependencies": {
    "live-server": "^1.2.1"
  }
}
